import { entityKind } from "drizzle-orm/entity.js";
import type { SQLiteDialectConfig } from "drizzle-orm/sqlite-core/dialect.js";
import { SQLiteDialect } from "drizzle-orm/sqlite-core/dialect.js";
import type { WithBuilder } from "drizzle-orm/sqlite-core/subquery.js";
import { WithSubquery } from "drizzle-orm/subquery.js";
import { SQLiteSelectBuilder } from "drizzle-orm/sqlite-core/query-builders/select.js";
import type { SelectedFields } from "drizzle-orm/sqlite-core/query-builders/select.types.js";
export declare class QueryBuilder {
    static readonly [entityKind]: string;
    private dialect;
    private dialectConfig;
    constructor(dialect?: SQLiteDialect | SQLiteDialectConfig);
    $with: WithBuilder;
    with(...queries: WithSubquery[]): {
        select: {
            (): SQLiteSelectBuilder<undefined, "sync", void, "qb">;
            <TSelection extends SelectedFields>(fields: TSelection): SQLiteSelectBuilder<TSelection, "sync", void, "qb">;
        };
        selectDistinct: {
            (): SQLiteSelectBuilder<undefined, "sync", void, "qb">;
            <TSelection extends SelectedFields>(fields: TSelection): SQLiteSelectBuilder<TSelection, "sync", void, "qb">;
        };
    };
    select(): SQLiteSelectBuilder<undefined, 'sync', void, 'qb'>;
    select<TSelection extends SelectedFields>(fields: TSelection): SQLiteSelectBuilder<TSelection, 'sync', void, 'qb'>;
    selectDistinct(): SQLiteSelectBuilder<undefined, 'sync', void, 'qb'>;
    selectDistinct<TSelection extends SelectedFields>(fields: TSelection): SQLiteSelectBuilder<TSelection, 'sync', void, 'qb'>;
    private getDialect;
}
